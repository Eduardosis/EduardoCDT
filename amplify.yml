version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd cdt-equipo8/frontend
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: cdt-equipo8/frontend/build
        files:
          - '**/*'
